# Copy to .env.local for local development.
# VITE_* values are inlined into the browser bundle at build time — this is the
# URL the *browser* uses to reach the API, not a server-internal address.
VITE_API_BASE_URL=http://localhost:8000
